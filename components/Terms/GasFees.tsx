
const GasFees = (): JSX.Element => (
  <>
    <strong>What are fas fees?</strong> It's the fee to send and purchase crypto. It helps keep the network safe and pays the network validators. Each "network" (crypto) has its own fee schedule.
  </>
);

export default GasFees;